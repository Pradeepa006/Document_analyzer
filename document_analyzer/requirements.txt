# ─── Web Framework ───────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.9      # Required for UploadFile support in FastAPI

# ─── Config & Env ────────────────────────────────────────────────────────────
pydantic==2.10.6
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ─── Async HTTP (Gemini API calls) ───────────────────────────────────────────
httpx==0.27.2
aiofiles==23.2.1

# ─── Document Parsing ────────────────────────────────────────────────────────
pdfplumber==0.11.4           # PDF text extraction (handles tables well)
python-docx==1.1.0           # DOCX text extraction

# ─── Embeddings (local, no API key needed) ───────────────────────────────────
sentence-transformers==3.2.1
torch==2.9.1                 # Required by sentence-transformers (CPU version)
# For GPU support, replace torch line with:
# torch==2.3.1+cu121 --index-url https://download.pytorch.org/whl/cu121

# ─── Vector Database ─────────────────────────────────────────────────────────
chromadb==0.5.4

# ─── Utilities ───────────────────────────────────────────────────────────────
numpy==1.26.4            # Tested with Python 3.12.x (do not use 3.13 yet)

# ─── Logging & Structured Logs ──────────────────────────────────────────────
loguru==0.7.2

# ─── Google Gemini SDK (Recommended instead of raw httpx calls) ─────────────
google-generativeai==0.8.3

# ─── Tokenization / Transformers (Stability for embeddings) ─────────────────
transformers==4.44.2
tokenizers==0.19.1

# ─── SciPy (Required by some embedding operations) ──────────────────────────
scipy==1.14.1

# ─── Typing & Validation Helpers ────────────────────────────────────────────
typing-extensions==4.12.2

# ─── Retry Handling (LLM calls reliability) ─────────────────────────────────
tenacity==9.0.0

# ─── JSON Optimization (Faster responses) ───────────────────────────────────
orjson==3.10.7

# ─── Production Server (Better than uvicorn alone) ──────────────────────────
gunicorn==23.0.0

# ─── Environment Management ─────────────────────────────────────────────────
python-decouple==3.8

# ─── Progress bars & Debugging ──────────────────────────────────────────────
tqdm==4.66.5
torch==2.9.1
sentence-transformers==3.2.1